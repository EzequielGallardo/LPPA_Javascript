/*1A) Crear 2 variables numéricas  y usar el operador suma
        para guardar el valor de la suma de ambos números en una
        tercer variable*/ 
        
        let vFirsrtNumber = 10;
        let vSecondNumber = 20;
        let vSuma = vFirsrtNumber + vSecondNumber;
        console.log (vSuma);

/*1B) Crear 2 variables de tipo STRING y concatenarlas
        para guardar el valor en una tercer variable*/ 
        
        let vFirsrtString = 'FIRST';
        let vSecondString = 'SECOND';
        let vConcatenacion = vFirsrtString + vSecondString;
        console.log (vConcatenacion);

/*1C) Crear 2 variables de tipo STRING y SUMAR la cantidad
        de caracteres de cada STRING, para guardar el valor en 
        una tercer variable*/ 
        
        let vSumaCaracteres = vFirsrtString.length +  vSecondString.length;
        console.log (vSumaCaracteres);